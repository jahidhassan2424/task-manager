import React from 'react';

const Loading = () => {
    return (
        <div className='[height:100vh]'>
            <h1 className='text-3xl text-center flex items-center'>Loading...</h1>
        </div>
    );
};

export default Loading;